# Assets

Shared media library for material used across more than one project. Assets belonging to a single project live instead in that project's own `05-assets/`.

- `images/`
- `documents/`
- `data/`
